import React from 'react'

const features = () => {
	return (
		<div>
			This is a simple todo application made with React + Redux. The back end is written using ExpressJS and the data is stored on a MongoDB Server.
		</div>
	)
}

export default features
